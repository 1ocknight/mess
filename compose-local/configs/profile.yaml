postgres:
  host: postgres
  port: 5432
  user: profile
  password: profile
  db_name: profile
  ssl_mode: disable

s3:
  client:
    region: us-east-1
    endpoint: http://minio:9000
    access_key_id: profile
    secret_access_key: profile-secret
    path_style: true
  bucket: avatar
  presign_duration: 1m

http: 
  host: 0.0.0.0 
  port: 8080
  debug_mode: true

verify:
  jwks_endpoint: http://keycloak:8080/auth/realms/main/protocol/openid-connect/certs

avatar_deleter:
  run_hour_utc: -1
  interval: 10m

profile_deleter:
  kafka:
    brokers:
    - kafka:29092
    topics:
    - keycloak-events
    - keycloak-admin-events
    group_id: profile-deleter
  delay: 10s

migrations_path: file://migrations
